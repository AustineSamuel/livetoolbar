import { View, Text } from 'react-native'
import React from 'react'
import ProfileMenu from '@/app_modules/User/components/profileMenu'

const Profile = () => {
  return (
   <ProfileMenu/>
  )
}

export default Profile